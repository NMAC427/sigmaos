load("@rules_go//go:def.bzl", "go_binary", "go_library")

go_library(
    name = "besched_lib",
    srcs = ["main.go"],
    importpath = "sigmaos/cmd/kernel/besched",
    visibility = ["//visibility:private"],
    deps = [
        "//debug",
        "//sched/besched/srv",
    ],
)

go_binary(
    name = "besched",
    embed = [":besched_lib"],
    visibility = ["//visibility:public"],
)
